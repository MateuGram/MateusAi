services:
  - type: web
    name: mateus-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python mateusai.py
    envVars:
      - key: ADMIN_PASSWORD
        sync: false
      - key: SECRET_KEY
        generateValue: true
    plan: free
